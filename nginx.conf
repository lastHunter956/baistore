server {
    listen 80;
    server_name _;

    root /app/public;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /static/ {
        alias /app/public/static/;
    }

    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
}